{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "preLaunchTask": "ng serve",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:4200/we",
            "webRoot": "${workspaceFolder}"
        }
    ]
}