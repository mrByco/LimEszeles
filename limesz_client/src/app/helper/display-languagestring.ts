

// TODO impplement language strings
/*import { LanguageString } from "../api/models";
import { LanguageService } from "../services/language.service";


export function display(languageString: LanguageString): string {
    let lang = LanguageService.SelectedLanguage;
    return languageString?.items?.find(i => i.code == lang)?.value ?? "";
}*/
