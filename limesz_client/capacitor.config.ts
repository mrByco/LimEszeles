import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.margarita_app',
  appName: 'Margareta',
  webDir: 'dist/margarita_app/browser',
  bundledWebRuntime: false
};

export default config;
